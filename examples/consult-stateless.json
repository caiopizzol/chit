{
	"schema": 1,
	"id": "consult-stateless",
	"description": "Ask Codex and Claude the same question in parallel, stateless (no session persistence).",

	"inputs": {
		"question": { "type": "string" }
	},

	"requires": {
		"can_show_markdown": true
	},

	"participants": {
		"codex": {
			"agent": "codex",
			"role": "Second opinion advisor. Be specific. Cite file:line when describing code.",
			"session": "stateless"
		},
		"claude": {
			"agent": "claude",
			"role": "Second opinion advisor. Be specific. Cite file:line when describing code.",
			"session": "stateless"
		}
	},

	"steps": {
		"ask_codex": {
			"call": "codex",
			"prompt": "{{ inputs.question }}"
		},
		"ask_claude": {
			"call": "claude",
			"prompt": "{{ inputs.question }}"
		},
		"out": {
			"format": "## codex\n\n{{ steps.ask_codex.output }}\n\n## claude\n\n{{ steps.ask_claude.output }}"
		}
	},

	"output": "out"
}
