{
	"schema": 1,
	"id": "ask-codex",
	"description": "Ask Codex a single stateless question.",

	"inputs": {
		"question": { "type": "string" }
	},

	"requires": {
		"can_show_markdown": true
	},

	"participants": {
		"codex": {
			"agent": "codex",
			"role": "Answer briefly. Cite file:line for any claim about code.",
			"session": "stateless"
		}
	},

	"steps": {
		"ask": {
			"call": "codex",
			"prompt": "{{ inputs.question }}"
		},
		"out": {
			"format": "{{ steps.ask.output }}"
		}
	},

	"output": "out"
}
