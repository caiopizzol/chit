{
	"id": "implementation-review",
	"description": "Implement a code slice, review it, and verify with checks until it passes.",
	"inputs": {
		"task": { "type": "string", "required": true, "description": "the slice to implement" }
	},
	"participants": {
		"builder": {
			"agent": "claude",
			"instructions": "You implement a small, well-scoped code slice and keep going until the checks pass.",
			"filesystem": "read-write"
		},
		"critic": {
			"agent": "claude",
			"instructions": "You review the work for correctness and scope. You do not edit files.",
			"filesystem": "read-only"
		}
	},
	"steps": [
		{
			"id": "build",
			"call": "builder",
			"prompt": "Task:\n{{ inputs.task }}\n\nIteration {{ iteration }}.\nPrior review:\n{{ steps.critique.output }}\n\nFailing checks from last iteration:\n{{ steps.verify.output }}\n\nMake the smallest change that moves toward passing the checks."
		},
		{
			"id": "critique",
			"call": "critic",
			"prompt": "Review the current diff for the task \"{{ inputs.task }}\":\n\n{{ diff }}\n\nCall out correctness and scope problems. Be brief. Do not edit files."
		},
		{
			"id": "verify",
			"check": [
				{ "command": "bun", "args": ["run", "typecheck"] },
				{ "command": "bun", "args": ["test"] }
			]
		}
	],
	"repeat": { "until": "checks-pass", "maxIterations": 3 },
	"limits": { "runTimeoutMinutes": 60 }
}
