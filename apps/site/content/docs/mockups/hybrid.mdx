---
title: C. Hybrid (recommended)
description: TypeTable for typed fields, custom visuals where they earn their place.
---

<Banner changeLayout={false}>Mockup C. TypeTable for the field references, custom visuals for the rest.</Banner>

## The schema, explained

Lead with the annotated example, so the shape lands before the field-by-field detail.

<AnnotatedConfig />

## Routine agent

A typed reference is exactly what `TypeTable` is for.

<TypeTable
	type={{
		profile: { type: "string", required: true, description: "Profile id from the root profiles object." },
		instructions: { type: "string", required: true, description: "Stable instructions for this agent in this routine." },
		filesystem: {
			type: '"none" | "read-only" | "read-write"',
			required: true,
			description: "File access. read-write runs in a disposable git worktree first.",
		},
	}}
/>

<FilesystemScale />

## Steps

<StepKindGrid />

## Loops

Tabs keep the three conditions to one block instead of three stacked code samples.

<Tabs items={["checks-pass", "step equals", "all"]}>
	<Tab>`"checks-pass"` is deterministic and the default. May omit the cap; it defaults to 5.</Tab>
	<Tab>`{ "step": "review", "equals": "pass" }` lets a verdict end the loop. The cap is required.</Tab>
	<Tab>`{ "all": ["checks-pass", { "step": "review", "equals": "pass" }] }` needs both. A reviewer can block convergence.</Tab>
</Tabs>

## How it runs

<DerivedBehavior />

## What the parser rejects

<RejectList />

<Callout title="Structural, not live">
	Validation proves the config is well-formed. It does not prove your account can use a model.
	`codex:gpt-5.5` is structurally valid but still fails if your local codex login lacks that model.
</Callout>
