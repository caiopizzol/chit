---
title: B. Custom brand visuals
description: The config reference rendered with custom paper-and-ink components.
---

<Banner changeLayout={false}>Mockup B. The same config slice, custom components in the chit palette.</Banner>

## The schema, explained

A single annotated example instead of a wall of fields. The code is on the left, what each block does is on the right.

<AnnotatedConfig />

## Filesystem

A scale, not a table row. Permission escalates left to right; the last one is sandboxed.

<FilesystemScale />

## Steps

Five kinds, as cards. The mono chip is the required fields.

<StepKindGrid />

## Loops

<LoopConditionCards />

## How it runs

<DerivedBehavior />

## What the parser rejects

Shape-coded, the same diamond the CLI uses for a failed check.

<RejectList />
