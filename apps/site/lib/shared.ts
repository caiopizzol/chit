export const appName = "chit";

// The marketing landing lives at `/`; docs live under `/docs`.
export const docsRoute = "/docs";

// Per-page raw Markdown for agents, e.g. /llms.mdx/concepts/content.md.
export const docsContentRoute = "/llms.mdx";

// Public origin, used for absolute metadata URLs (sitemap, canonical, OG).
export const siteUrl = "https://chit.run";

export const gitConfig = {
	user: "caiopizzol",
	repo: "chit",
	branch: "main",
};
