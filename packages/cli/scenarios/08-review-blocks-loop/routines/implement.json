{
	"id": "implement",
	"description": "Advanced + fragile: converge only when checks pass AND a constrained model verdict returns pass.",
	"inputs": {
		"task": { "type": "string", "required": true }
	},
	"participants": {
		"builder": {
			"agent": "builder",
			"instructions": "Implement the task and revise from reviewer feedback.",
			"filesystem": "read-write"
		},
		"critic": {
			"agent": "critic",
			"instructions": "You review diffs. You do not edit files.",
			"filesystem": "read-only"
		}
	},
	"steps": [
		{ "id": "build", "call": "builder", "prompt": "Task:\n{{ inputs.task }}\n\nIteration {{ iteration }}.\nReview feedback:\n{{ steps.review.output }}\nCheck feedback:\n{{ steps.verify.output }}" },
		{ "id": "review", "call": "critic", "prompt": "Task:\n{{ inputs.task }}\n\nDiff:\n{{ diff }}\n\nGive concrete, specific feedback to improve correctness and scope. Free-form; this is guidance for the builder, not a verdict." },
		{ "id": "verdict", "call": "critic", "prompt": "Task:\n{{ inputs.task }}\n\nDiff:\n{{ diff }}\n\nReply with exactly one word: pass (if correct, in scope, and ready to ship) or revise (otherwise). Nothing else." },
		{ "id": "verify", "check": [{ "command": "node", "args": ["-e", "console.log('ok')"] }] }
	],
	"repeat": { "until": { "all": ["checks-pass", { "step": "verdict", "equals": "pass" }] }, "maxIterations": 3 }
}
