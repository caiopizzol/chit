{
	"routines": {
		"implement": {
			"file": "routines/implement.json",
			"description": "Build, review, and verify in a sandbox.",
			"defaults": { "maxIterations": 3 }
		}
	},
	"profiles": {
		"builder": { "adapter": "codex", "model": "gpt-5.5" },
		"critic": { "adapter": "gemini", "model": "default" }
	}
}
