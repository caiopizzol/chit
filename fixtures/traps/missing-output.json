{
	"schema": 1,
	"id": "trap-missing-output",
	"description": "output names a step that does not exist.",
	"inputs": { "q": { "type": "string" } },
	"participants": {
		"advisor": { "agent": "claude", "instructions": "Advisor.", "session": "stateless" }
	},
	"steps": { "ask": { "call": "advisor", "prompt": "{{ inputs.q }}" } },
	"output": "ghost"
}
