{
	"schema": 1,
	"id": "trap-unknown-agent",
	"description": "Participant points at an agent that is not in the registry.",
	"inputs": { "q": { "type": "string" } },
	"participants": {
		"advisor": { "agent": "gpt-9000", "instructions": "Advisor.", "session": "stateless" }
	},
	"steps": { "ask": { "call": "advisor", "prompt": "{{ inputs.q }}" } },
	"output": "ask"
}
