node_modules/
.DS_Store
*.log
mockups/

# Cloudflare Wrangler local state
.wrangler/


# Studio client bundle output (generated by bun run build:client)
apps/studio/dist/

# Playwright e2e artifacts
apps/studio/test-results/
apps/studio/playwright-report/
apps/studio/.playwright/

# Local Claude Code runtime state (scheduled-task lock, etc.)
.claude/

# Convergence loop logs (chit loop-log / chit converge write here). Product
# summaries, but they carry task/change text and may reference sensitive
# content; never commit them. Full audit transcripts live in ~/.local/state.
.chit/
